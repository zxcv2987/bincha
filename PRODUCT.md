# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

현재 유일한 사용자는 개발자 본인(taeYang)이다. 로그인이 비밀번호 단일 필드뿐이고(사용자명 없음), 1인 전용 개인 도구로 운영 중이다.

`docs/product-plan.md`가 정의하는 장기 타겟(개발자, 취업 준비생, 이직 준비자, 개인 프로젝트 진행자, 업무 회고를 남기고 싶은 사람)은 향후 방향이며, 아직 그 사용자들에게 열려 있지 않다.

## Product Purpose

Bincha는 할 일을 끝내는 데서 멈추지 않고, 실행 결과와 근거를 기록해 장기적으로 활용 가능한 성과로 전환하는 개인 작업 관리 도구다.

일반 Todo 앱은 완료된 작업이 체크 표시로만 남고, 실제로 무엇을 했는지·무엇이 달라졌는지·왜 그 방법을 선택했는지는 사라진다. Bincha는 이 기록 공백을 해결한다.

## Positioning

핵심 메시지: "완료한 일은 사라지지 않고 성과가 된다."

경쟁 Todo 앱이 그대로 베낄 수 없는 지점은 파이프라인 자체다: `할 일 등록 → 완료 → 결과 기록 → 성과 후보 → 측정 → 성과 확정`. 특히 "결과"(실제 수행 내용)와 "성과"(그 결과가 만든 가치)를 구조적으로 분리하고, 수치 근거가 없는 성과는 확정하지 않는다는 원칙이 다른 생산성 도구와 다른 점이다.

## Operating Context

- 개발자가 업무/개인 프로젝트에서 할 일을 카테고리별로 관리하는 흐름
- 할 일 완료 직후 "결과 기록" 또는 "나중에" 선택 — 완료를 결과 입력으로 막지 않음
- 결과 기록 대기 항목은 "결과함"에 모여 나중에 채울 수 있음
- 이력서, 포트폴리오, 면접 답변, 주간/월간/연말 회고에 쓸 재료를 축적하는 것이 최종 목적(장기 로드맵, MVP 범위 밖)

## Capabilities and Constraints

**현재 구현 범위(MVP)**
- 카테고리 생성/이름 수정/삭제/순서 변경 (사용자별 소유, 이름 중복 불가, 순서는 서로 다른 사용자에 영향 없음)
- 할 일 생성/수정/삭제/완료·완료 취소, 완료일 기록
- `task_result`로 완료된 할 일과 1:1 결과 기록: 요약(필수), 변화한 점/예상과 다른 점/다음 행동/근거 URL(모두 선택), 측정 필요 여부 체크(값만 저장, 측정 기능 자체는 미구현)
- 결과 없는 할 일은 바로 삭제 가능, 결과 있으면 삭제 전 경고
- 카테고리 삭제는 cascade 금지 — 안의 할 일을 이동하거나 삭제를 취소해야 함

**제약 및 확장 여부**
- 스택(Next.js 16 App Router / React 19 / TypeScript / Prisma + Supabase Postgres / Zustand / Vercel 배포)이나 비밀번호 단일 인증 방식을 지켜야 할 의무는 없음 — 필요하면 자유롭게 바꿀 수 있음(사용자 확인 완료)
- Achievement(성과) / Measurement(측정) / Evidence(근거 타입) 모델은 아직 미구현. 결과 기록이 몇 주 쌓여 "여러 결과를 하나의 성과로 묶고 싶다"는 신호가 실사용으로 확인된 뒤 설계 예정 — 지금 스키마를 미리 확장하지 않는다
- 명시적 비목표: 팀 협업, 조직 권한 관리, 실시간 공동 편집, 칸반 보드, 간트 차트, 하위 작업, 반복 일정, 업무 시간 측정, Slack/Notion 양방향 동기화, GitHub PR 자동 수집, AI의 완전 자동 성과 확정

## Brand Commitments

- 제품명 "빈차" — 택시 "빈차등"(vacancy light)에서 따온 이름
- 헤더 타이틀 "내가 해야 할 일", 서브카피 "벌어야 할 돈 말고도 뭐가 있었는데"
- 브랜드 컬러는 oklch 기반 `brand-*` 토큰(`styles/globals.css`)으로 정의되어 있고, 대비비를 실측해 문서화해 둠(white on brand-600 = 5.45:1 등) — 임의로 바꾸지 말고 이 토큰을 따를 것

## Evidence on Hand

- 실배포 중: https://bincha.vercel.app/
- 상세 기획/기술 문서 보유: `docs/mvp-plan.md`(1차 검증 범위), `docs/product-plan.md`(최종 방향), `docs/mvp-technical-plan.md`, `docs/technical-proposal.md`
- 실사용자 후기, 정량 지표(주간 완료 수, 결과 기록률 등)는 아직 없음 — 지어내지 말 것. `docs/mvp-plan.md` 9장에 목표 지표만 정의되어 있고 실측치는 없다.

## Product Principles

1. **할 일 등록은 가볍다** — 필수 정보는 제목+카테고리뿐, 설명은 선택
2. **완료를 방해하지 않는다** — 완료 시 결과 입력을 강제하지 않고 "결과 기록" / "나중에" 선택지를 준다
3. **결과와 성과를 구분한다** — 결과는 실제 수행 내용, 성과는 그 결과가 만든 가치
4. **성과를 과장하지 않는다** — 초안 → 측정 중 → 확인됨 단계를 거치지 않은 변화는 성과로 확정하지 않는다
5. **사용자가 최종 판단한다** — AI가 도입되어도 결과나 성과를 자동으로 확정하지 않는다(AI 제안 → 사용자 확인 → 사용자 수정 → 저장)

## Accessibility & Inclusion

제품 차원의 공식 접근성 기준(WCAG 등급 등)은 아직 정해져 있지 않다(사용자 확인 완료 — 필요할 때 케이스별로 판단). 다만 최근 세션에서 포커스 인디케이터·텍스트 대비 개선 작업이 진행 중이며(2026-07-29 `/impeccable audit` 결과 참고), 관행적으로 WCAG AA를 참고선으로 삼고 있다.
