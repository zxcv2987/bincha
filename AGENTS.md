# Bincha Agent Instructions

## Non-Negotiables

1. 확실하지 않으면 추측하지 말고 관련 파일을 읽거나 명령을 실행한다.
2. 사용자의 전제가 틀렸으면 먼저 짚고, 결과를 크게 바꾸는 모호함은 구현 전에 확인한다.
3. 요청과 직접 연결되지 않은 리팩터링, 포맷 변경, 기능 확장을 하지 않는다.
4. 변경 전에는 대상 파일, 호출부, 관련 타입과 테스트를 먼저 확인한다.
5. 의미 있는 변경은 검증 기준을 먼저 정하고 실행 결과로 닫는다.
6. 실패하거나 실행하지 못한 검증은 숨기지 않고 원인과 남은 위험을 최종 보고에 적는다.

## Project Context

- Next.js 16 App Router, React 19, TypeScript 프로젝트다.
- 패키지 매니저는 `pnpm`이며 `package.json`의 `packageManager` 버전을 따른다.
- 데이터 접근은 Prisma와 Supabase Postgres를 사용한다.
- 앱 라우트는 `app/`, 기능 코드는 `features/`, 공통 코드는 `lib/`와 `providers/`에 둔다.
- 데이터 모델 변경 전에는 `prisma/schema.prisma`와 관련 마이그레이션을 먼저 확인한다.
- 인증, 사용자 소유권, RLS 변경은 신뢰 경계를 바꾸는 작업으로 취급하고 다른 사용자의 데이터 접근 차단을 검증한다.
- 비밀값을 저장소에 넣지 않는다. 새 환경 변수를 도입하면 실제 값 없이 `.env.example`도 함께 갱신한다.

## Documentation

작업에 필요한 문서만 읽는다.

- 현재 MVP 제품 범위와 사용자 흐름: `docs/mvp-plan.md`
- 현재 MVP 구현 계약, 데이터 모델, 정책, 테스트 범위: `docs/mvp-technical-plan.md`
- 장기 제품 방향과 MVP 이후 우선순위: `docs/product-plan.md`
- 장기 기술 방향과 MVP 이후 아키텍처: `docs/technical-proposal.md`
- 문서 안내: `docs/index.md`

제품 동작, 기능 범위, 사용자 흐름 또는 우선순위에 영향을 주는 변경 전에는 `docs/mvp-plan.md`를 먼저 읽는다. MVP 범위를 넘어서는 제안이나 변경이라면 `docs/product-plan.md`도 확인한다.

데이터 모델, 인증, 사용자 데이터 경계, 캐시, 삭제 정책 또는 서비스 계약을 바꾸기 전에는 `docs/mvp-technical-plan.md`를 먼저 읽는다. MVP 이후 구조를 다루는 경우에만 `docs/technical-proposal.md`도 확인한다.

코드와 문서의 계약이 달라지는 변경은 같은 작업에서 관련 문서를 갱신한다. 내부 구현만 바뀌고 문서화된 동작이나 계약이 그대로라면 불필요한 문서 수정은 하지 않는다.

## Working Rules

- 가장 작은 변경으로 작업을 닫고 기존 코드 스타일과 구조를 따른다.
- 커밋 메시지는 `영문 접두사: 한국어 설명` 형식으로 작성한다. 예: `feat: 결과 기록 기능 추가`, `fix: 선택 상태 표시 오류 수정`, `docs: 테스트 절차 문서화`.
- 큰 의존성은 명확한 필요성과 비용 설명 없이 추가하지 않는다.
- 관련 없는 죽은 코드나 구조 문제는 삭제하지 않고 필요하면 최종 보고에 별도로 남긴다.
- UI 변경에는 접근성, 로딩·오류·빈 상태, 반응형 동작에 미치는 영향을 확인한다.
- Server Action이나 서비스 변경에는 입력 검증, 인증, 사용자 소유권, 캐시 무효화에 미치는 영향을 확인한다.
- Prisma 스키마 변경은 대응하는 마이그레이션과 테스트 전략을 함께 다룬다.

## Tests and Completion Criteria

- 동작을 추가하거나 바꾸거나 버그를 수정할 때는 관련 테스트를 먼저 찾는다.
- 테스트 가능한 동작 변경에는 기존 테스트를 수정하거나 회귀 테스트를 추가한다. 버그 수정은 가능하면 수정 전 실패하는 테스트로 문제를 재현한다.
- 테스트가 실익이 없는 문서·스타일·정적 설정 변경에는 억지 테스트를 만들지 않는다.
- 구현 후 변경 범위에 맞는 검증을 실행한다. 테스트나 문서 보완까지 끝나야 구현이 완료된 것으로 본다.
- 테스트를 추가할 수 없거나 실행할 수 없으면 이유와 수동 검증 방법, 남은 위험을 최종 보고에 적는다.
- TDD 범위의 구현을 완료한 경우 서브 에이전트에게 변경 내용과 테스트를 리뷰받고, 지적 사항을 반영하거나 반영하지 않은 이유를 기록한다.

## Commands

- 개발 서버: `pnpm dev`
- 린트: `pnpm lint`
- 타입 검사: `pnpm exec tsc --noEmit`
- 서비스 통합 테스트: `pnpm test`
- 프로덕션 빌드: `pnpm build`

`pnpm test`는 `.env.test`와 분리된 테스트 스키마가 필요하다. 설정과 마이그레이션 절차는 `docs/mvp-technical-plan.md`의 테스트 섹션을 따른다.

## Verification

- 문서만 변경: 링크, 파일 경로, 코드·현재 범위와의 일치를 직접 확인한다.
- JS/TS 내부 로직 변경: `pnpm exec tsc --noEmit`과 관련 테스트를 실행한다.
- UI 또는 상태 변경: `pnpm lint`, `pnpm exec tsc --noEmit`을 실행하고 영향을 받는 흐름을 확인한다.
- 서비스, 인증, 소유권 또는 데이터 정책 변경: 위 검증에 더해 `pnpm test`를 실행한다.
- Prisma 스키마나 빌드 설정 변경: 위 검증에 더해 가능하면 `pnpm build`를 실행하고 마이그레이션 적용 가능성을 확인한다.

전체 저장소 검증이 변경과 무관한 기존 문제로 실패하면 해당 문제를 고치기 위해 범위를 넓히지 말고 실패 원인과 이번 변경에 미치는 영향을 보고한다.

## Project Learnings

- 테스트의 `it(...)`와 `test(...)` 설명은 한국어 문장으로 작성한다. 고유 기술 용어는 원문 표기를 유지할 수 있다.
